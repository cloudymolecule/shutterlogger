<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <title>Shutterlogger</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>
  <body>
      <% if logged_in? %>
        <a href="/users/<%= current_user.id %>">HOME</a>
        <a href="/logout">LOG OUT</a>
        <h1><%= "#{current_user.username}'s ShutterLogger" %></h1><br /><br />
        <% else %>
        <a href="/">HOME</a>
        <h1>ShutterLogger</h1><br /><br />
      <% end %>
      
        <%= yield %>
  </body>
</html>
